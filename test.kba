; This is a simple program
num1:   wd      5
num2:   wd      0

        in

start_loop:
        dup
        jif     add_number
        jmp     end

add_number:
        dec
        ld      num2
        inc
        dup
        out
        st      num2
        jmp     start_loop

end:
        halt
