; This is a simple program
num2:   wd      0

        in

start_loop:
        dup
        ld      num2
        iseq
        jif     end

add_number:
        minc    num2
        ld      num2
        out
        jmp     start_loop

end:
        halt
