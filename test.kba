; This is a simple program
num2:   wd      0

        in

start_loop:
        dup
        ld      num2
        iseq
        jif     end

add_number:
        ld      num2
        inc
        dup
        out
        minc    num2
        jmp     start_loop

end:
        halt
