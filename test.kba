; This is a simple program
num1:   w       5
num2:   w       0

        ld      num1

start_loop:
        dup
        out
        dup
        jif     dec
        jmp     end

dec:
        dec
        jmp     start_loop

end:
        halt

